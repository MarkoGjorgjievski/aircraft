export class Flight {
  constructor(aircraftType, flightNumber, destination, flyingTime) {
    this.aircraftType = aircraftType;
    this.flightNumber = flightNumber;
    this.destination = destination;
    this.flyingTime = flyingTime;
  }
}
